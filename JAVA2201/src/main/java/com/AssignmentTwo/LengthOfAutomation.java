package com.AssignmentTwo;

import sun.security.util.Length;

public class LengthOfAutomation {
    public static void main(String[] args) {
        String s = "Automation";

        //initialize a count variable which will store the length.

        int count = s.length();

        //print the count variable or the length of string.

        System.out.println("length of the word is " + count);
        }
    }

